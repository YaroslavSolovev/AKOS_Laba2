# Ping-Pong Project: Team Work Distribution

## Описание проекта

Упрощенная реализация межпроцессного взаимодействия через файл-дескриптор. Клиент отправляет `ping`, сервер отвечает `pong`.

## Ключевые особенности

### ✨ Упрощенная архитектура:
- **Простой формат сообщений:** просто `ping` и `pong` (БЕЗ ID и timestamp)
- **Минимальное логирование:** без временных меток, только роль + сообщение
- **Строгая валидация:** сервер принимает ТОЛЬКО `ping`
- **Интерактивный клиент:** пользователь вводит команды в консоли

### 🏗️ Архитектура:
- **Базовый класс:** `BaseCommunicator` с общими методами работы с файлом
- **Клиент:** `PingPongClient` - отправка запросов и получение ответов
- **Сервер:** `PingPongServer` - обработка запросов (только `ping`)
- **Состояния:** `ClientState` и `ServerState` для управления жизненным циклом
- **Исключения:** Иерархия ошибок для обработки различных ситуаций

## Командная работа

**Scope:** ~530 lines of code across 15 files
**Team Size:** 5 developers
**Architecture:** Client-server model with state management

### Распределение:

1. **Person 1** (100 lines): Базовая инфраструктура + исключения
2. **Person 2** (80 lines): Оставшиеся исключения + состояния клиента
3. **Person 3** (80 lines): Клиент - создание и отправка запросов
4. **Person 4** (100 lines): Клиент - обработка ответов + интерактивное приложение
5. **Person 5** (170 lines): Сервер + состояния + приложение

### Зависимости:
- Persons 1-2 работают параллельно (фундамент)
- Persons 3-5 начинают после завершения 1-2

## Критерии успеха

Проект считается успешным когда:
1. Сервер запускается и ждет запросов
2. Клиент интерактивно принимает команды от пользователя
3. На `ping` клиент получает `pong`
4. На другие команды клиент получает ошибку от сервера
5. Graceful shutdown работает корректно (Ctrl+C, exit)

## Использование

**Терминал 1 (Сервер):**
```bash
python server_app.py
```

**Терминал 2 (Клиент):**
```bash
python client_app.py
> ping
[CLIENT] > ping
[CLIENT] < pong

> hello
[CLIENT] > hello
[CLIENT] Ошибка: Сервер: ожидается 'ping'

> exit
Выход...
```

## Технические требования

- Python 3.7+
- ООП стиль
- PEP 8 compliance
- Type hints
- Docstrings для всех методов
- Минимальное логирование (без timestamp)
