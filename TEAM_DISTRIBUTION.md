# Распределение работы по проекту Ping-Pong (упрощенная версия)

## Команда из 5 человек

---

## 👤 Человек 1: Базовая инфраструктура и ошибки

**Ответственность:** Базовый класс и система обработки ошибок

**Файлы:**
- `src/base_communicator.py`
- `src/errors/__init__.py`
- `src/errors/communication_error.py`
- `src/errors/file_access_error.py`

**Задачи:**
1. Создать абстрактный базовый класс `BaseCommunicator`
   - Методы работы с файлом (_read_file, _write_file, _clear_file, _remove_file)
   - Метод логирования (_log) **БЕЗ временных меток**
   - Абстрактный метод _change_state
2. Создать базовое исключение `CommunicationError`
3. Создать исключение `FileAccessError` для ошибок работы с файлом
4. Написать docstring для всех методов

**Объем работы:** ~100 строк кода

---

## 👤 Человек 2: Исключения и состояния клиента

**Ответственность:** Оставшиеся исключения и состояния клиента

**Файлы:**
- `src/errors/timeout_error.py`
- `src/errors/invalid_request_error.py`
- `src/errors/invalid_response_error.py`
- `src/states/__init__.py`
- `src/states/client_state.py`

**Задачи:**
1. Создать исключение `TimeoutError` для таймаута ожидания
2. Создать исключение `InvalidRequestError` для неверных запросов
3. Создать исключение `InvalidResponseError` для неверных ответов
4. Создать перечисление `ClientState` с состояниями клиента:
   - IDLE, CREATING_REQUEST, WAITING_RESPONSE, READING_RESPONSE, ERROR, COMPLETED
5. Написать docstring с описанием переходов состояний

**Объем работы:** ~80 строк кода

---

## 👤 Человек 3: Клиент (часть 1)

**Ответственность:** Клиент - создание и отправка запросов

**Файлы:**
- `src/client.py` (первая часть)

**Задачи:**
1. Создать класс `PingPongClient` (наследуется от BaseCommunicator)
2. Реализовать `__init__` (инициализация БЕЗ request_id)
3. Реализовать `_change_state` (БЕЗ логирования переходов)
4. Реализовать `create_request` (простой формат - БЕЗ ID и timestamp)
5. Реализовать `send_request` (отправка с логом `> message`)
6. Реализовать `wait_for_response` (проверка что содержимое != запросу)
7. Написать docstring для всех методов

**Объем работы:** ~80 строк кода

---

## 👤 Человек 4: Клиент (часть 2) + интерактивное приложение

**Ответственность:** Клиент - чтение ответов и интерактивное приложение

**Файлы:**
- `src/client.py` (вторая часть)
- `client_app.py`

**Задачи:**
1. Реализовать `read_response` (с проверкой на `ERROR:` от сервера)
2. Реализовать `handle_error` (БЕЗ retry логики)
3. Реализовать `cleanup` (очистка файла)
4. Реализовать `send_message` (главный метод - полный цикл)
5. Создать интерактивное приложение `client_app.py`:
   - Цикл с `input()` для ввода команд
   - Команды выхода: exit, quit, q
   - Graceful shutdown

**Объем работы:** ~100 строк кода

---

## 👤 Человек 5: Сервер + состояния сервера + точка входа

**Ответственность:** Полная реализация сервера

**Файлы:**
- `src/states/server_state.py`
- `src/server.py`
- `server_app.py`

**Задачи:**
1. Создать перечисление `ServerState` с состояниями сервера:
   - IDLE, WAITING_REQUEST, PROCESSING_REQUEST, SENDING_RESPONSE, ERROR, COMPLETED
2. Создать класс `PingPongServer` (наследуется от BaseCommunicator)
3. Реализовать все методы сервера:
   - `__init__` (инициализация БЕЗ last_request_id)
   - `_change_state` (БЕЗ логирования)
   - `wait_for_request` (ожидание запроса)
   - `process_request` (**принимает ТОЛЬКО "ping"**, иначе ValueError)
   - `send_response` (отправка ответа)
   - `handle_error` (обработка ошибок)
   - `process_single_request` (обработка одного запроса + отправка ERROR клиенту)
   - `start` (главный цикл сервера)
   - `stop` (остановка сервера)
4. Создать точку входа `server_app.py`

**Объем работы:** ~170 строк кода

---

## 📊 Сводка по объему работы:

| Человек | Ответственность | Строк кода | Файлов |
|---------|----------------|------------|--------|
| 1 | Базовая инфраструктура + ошибки | ~100 | 4 |
| 2 | Исключения + состояния клиента | ~80 | 5 |
| 3 | Клиент (создание запросов) | ~80 | 1 |
| 4 | Клиент (ответы) + интерактивное app | ~100 | 2 |
| 5 | Сервер + состояния + app | ~170 | 3 |

**Итого:** ~530 строк кода, 15 файлов

---

## 🔄 Порядок разработки (зависимости):

1. **Человек 1** начинает первым - создает базу
2. **Человек 2** может работать параллельно с Человеком 1
3. **Человек 3, 4, 5** начинают после завершения работы Человека 1 и 2

---

## ✅ Критерии приемки:

### Для всех:
- Код написан в ООП стиле
- Все методы имеют docstring
- Типы параметров указаны (type hints)
- Код следует PEP 8
- Логирование БЕЗ временных меток (только роль + сообщение)

### Человек 1:
- BaseCommunicator корректно работает с файлами
- Исключения правильно наследуются
- Логи без timestamp

### Человек 2:
- Все исключения созданы
- Состояния клиента описаны

### Человек 3:
- Клиент может создавать и отправлять простые запросы
- Метод wait_for_response не читает свой же запрос
- НЕТ request_id и timestamp

### Человек 4:
- Клиент распознает ошибки от сервера (ERROR:)
- Интерактивный режим с input()
- Graceful shutdown

### Человек 5:
- Сервер принимает ТОЛЬКО "ping"
- На другие запросы отправляет ERROR клиенту
- Очистка файла после каждого ответа
- Graceful shutdown (Ctrl+C)

---

## 🧪 Интеграционное тестирование:

После завершения всех частей:
1. Запустить `python server_app.py`
2. В другом терминале: `python client_app.py`
3. Ввести в клиенте:
   - `ping` → должен получить `pong`
   - `hello` → должен получить ошибку
   - `exit` → выход

**Ожидаемый результат:**
```
> ping
[CLIENT] > ping
[CLIENT] < pong

> hello
[CLIENT] > hello
[CLIENT] Ошибка: Сервер: ожидается 'ping'
```

---

## 📝 Примечания:

- Используйте Git для совместной работы
- Каждый коммитит только свои файлы
- Не изменяйте чужие файлы без согласования
- Регулярно делайте pull перед началом работы

## 🎯 Ключевые упрощения:

1. **Убраны временные метки** из логов
2. **Убраны request_id и timestamp** из сообщений
3. **Простой формат:** `ping` → `pong`
4. **Сервер строгий:** принимает только `ping`
5. **Интерактивный клиент:** пользователь вводит команды
6. **Минимальное логирование:** только `>` и `<`
